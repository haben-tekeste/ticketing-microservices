export enum Subjects {
    TicketCreated = 'ticket.created',
    TicketUpdated = 'ticket.updated',
    EventTicketCreated = "events.ticket.created",
    EventTicketUpdated = "events.ticket.updated"

}
